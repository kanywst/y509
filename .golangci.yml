linters:
  enable:
    - errcheck
    - gosimple
    - govet
    - staticcheck
    - unused
    - typecheck
    - revive
    - gofmt
    - misspell

run:
  timeout: 5m

issues:
  exclude-use-default: false
  max-issues-per-linter: 0
  max-same-issues: 0
